import React from 'react'
import PropTypes from 'prop-types'

const SavePlaylistButtons = props => {
    return (
        <div>SavePlaylistButtons</div>
    )
}

SavePlaylistButtons.propTypes = {}

export default SavePlaylistButtons