version: '3.4'

services:
  spotifydwapi:
    image: spotifydwapi
    build:
      context: .
      dockerfile: ./Dockerfile
    command: ["sh", "-c", "pip install debugpy -t /tmp && python /tmp/debugpy --wait-for-client --listen 0.0.0.0:5678 /app/src/main.py"]
    ports:
      - 8001:8001
      - 5678:5678
    env_file:
      - .env
    volumes:
      - ./data:/app/data
